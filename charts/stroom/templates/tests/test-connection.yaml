apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "stroom.fullname" . }}-test-connection"
  labels:
    {{- include "stroom.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "stroom.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
